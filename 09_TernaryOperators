// Conditionals: Ternary Operator

// condition ? ifTrue : ifFalse;

// let soup = "Chicken Noodle Soup";
let soup;					// undefined variable here. Considered false.

let response = soup
    ? "Yes, we have soup on the menu."
    : "Sorry, no soup on menu";			

console.log(response);				// "Sorry, no soup on menu"



let soup = "Chicken Noodle Soup";
// let soup;

let response = soup
    ? "Yes, we have soup on the menu."
    : "Sorry, no soup on menu";

console.log(response);				// "Yes, we have soup on the menu."



// Conditionals: Chained Ternary Operator

// condition ? ifTrue : ifFalse;

let soup = "Chicken Noodle Soup";
let isCustomerBanned = true;
let soupAccess = isCustomerBanned
    ? "Sorry, no soup for you b/c your banned!"
    : soup
    ? `Yes, we have ${soup} on the menu today.`
    : "No soup on the menu today.";

console.log(soupAccess);


// Variations Here

let soup = "Chicken Noodle Soup";
let isCustomerBanned = true;			
console.log(soupAccess);			// "Sorry, no soup for you b/c your banned!"

let soup = "Chicken Noodle Soup";
let isCustomerBanned = false;		// `Yes, we have ${soup} on the menu today.`
console.log(soupAccess);			// `Yes, we have Chicken Noodle Soup on the menu today.`
 
let soup;
let isCustomerBanned = false;			
console.log(soupAccess);			// "No soup on the menu today."
