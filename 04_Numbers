note AltFileBackArrow allows additional keyboard Move access
using keyboard navigate to VSCode 04tut folder (Cntl`), $ ls (to establish landscape)
AltTab to VSCode
using VSCode AltFile copy same file tree structure from 03tut to 04tut
only file we change is main.js
Cntl0 to navigate to file tree and then js folder and open main.js in editor
clear contents of main.js
keyboard navigate to Chrome, open new tab, close old tab  (Cntl 1, 2)
launch Live Server (CntlShiftP Search Functionality, Open Live Server AltLO, Close Live Server AltLC)
ALT Tab to browser, keep only generic original new tab, close all others
ALT Tab to VSCode, clear VSCode terminal, $ ls
in VSCode, $ start index.html (which launches rendered web page), CntlShiftI to lauch DevTools Console


// Numbers
console.log("Hello World Ch4");
const myNumber = 42;
const myString = "49";

// Floating Point number with a decimal point is a float
const myFloat = 42.001;

console.log(myNumber);			// notice purple color for number
console.log(myString);			// notice while color for string
console.log(myFloat);

console.log(myNumber === myString);
console.log(typeof(myString));
console.log(myString + 3);		// concatentates to 423
console.log(Number(myString));		// 42
console.log(Number(myString) + 3);	// 45
console.log(Number(myString) === myNumber);	//  true
console.log(Number("Dave"));		// NaN
console.log(Number(undefined));		// NaN
console.log(Number(true));		
console.log(Number(false));		
console.log(Number.isInteger(myFloat));
console.log(Number.isInteger(myNumber));
console.log(Number.parseFloat(myString));

const myString2 = "42.123abc";
console.log(Number.parseFloat(myString2));
console.log(Number.parseFloat(myString2).toFixed(1));
console.log(Number.parseFloat(myFloat).toFixed(1));
console.log(Number.parseFloat(myFloat).toFixed(5));

console.log(Number.parseInt(myFloat));
console.log(Number.parseInt(myString));

console.log(myFloat.toString());	// turns float into string
console.log(typeof Number.parseInt(myString));
console.log(typeof(Number.parseInt(myString)));
console.log(typeof(Number.parseFloat(myString)));

console.log(Number.parseFloat("4.237abc"));			// 4.237
console.log(typeof Number.parseFloat("4.237abc"));		// number
console.log(Number.parseFloat("4.237abc").toFixed(2));		// 4.24
console.log(typeof Number.parseFloat("4.237abc").toFixed(2));	// string

console.log(Number(4.237));		// 4.237
console.log(Number("4.237"));		// 4.237
console.log(Number("4.237abc"));	// Nan

// NaN Not a Number

console.log(Number("Atul"));		// NaN
console.log(Number.isNaN("Atul"));	// false
console.log(isNaN("Atul"));		// Global NaN  true
